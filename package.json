{
	"name": "libraw-wasm",
	"version": "1.1.2",
	"description": "LibRaw WASM bindings to be able to use it in the browser.",
	"main": "dist/index.js",
	"type": "module",
	"directories": {
		"lib": "lib"
	},
	"scripts": {
		"example": "node simple-server.js && open http://localhost:9001/example/example.html",
		"build": "node build.js"
	},
	"files": [
		"dist/*"
	],
	"author": "ybouane",
	"license": "ISC",
	"devDependencies": {
		"esbuild": "^0.24.2"
	}
}